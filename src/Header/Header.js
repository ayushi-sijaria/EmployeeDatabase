import React from 'react'
import classes from './Header.module.css'

const Header = () => {
     return (
          <div className={classes.Header}>Employee Database</div>
     )
}

export default Header
